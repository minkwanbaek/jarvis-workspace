# 2026-03-13

- mk set a default email-address rule: whenever mk says to send something to "my email", use `javajinx7@gmail.com` unless a different address is explicitly given.
- mk clarified file delivery preference: generated files/documents should be sent both by email and in chat by default.
- mk set image-generation resolution discipline: drafts should stay at 1K by default, and 4K must only be used when mk explicitly asks for it.
- Proposal-skill work now includes Samsung SDS-style `master-pack` structure in addition to tone-packs; cover-dark + body-light split is the preferred baseline for Samsung SDS proposal decks.
- OpenClaw secret handling was migrated away from plaintext config toward `~/.openclaw/.env` plus service env injection, and the migration was validated with gateway restart, email test, and Gemini/Nano Banana run.
- mk asked to revisit the self-healing OpenClaw/home-server use case and use it to refine Jarvis's operating model as an ongoing operational coordinator rather than a turn-by-turn chatbot.
- SOUL.md was updated to the balanced dedicated-secretary version.
- AGENTS.md was updated into a clearer operating charter focused on continuity, responsibilities, operational posture, and durable principles.
- Naming preference updated: assistant English name is `Jarvis`, Korean name is `자비스`.
- User address preference updated: call mk `엠케이님`.
- Added a system cron entry to run `scripts/publish_workspace_core.sh` daily at 19:00 UTC, logging to `workspace/logs/publish_workspace_core.log`.
