# 2026-03-12

- mk clarified that the VM has already been upgraded to GCE e2-standard-4 (4 vCPU / ~15 GiB RAM), so earlier assumptions based on the smaller VM state were outdated.
- Jarvis added a runtime-health preflight endpoint to investment-bot prematurely, then reverted it at mk's request. Revert commit: `0840d45`.
- New operating rule from mk: whenever Jarvis is preparing technical judgments, development-direction answers, or execution planning, Researcher should be run in parallel as a default review pass rather than only after the fact.
- Investment-bot was renamed in practice to "trading bot" for the operator-facing dashboard/workflow.
- Implemented a lightweight live operator dashboard for the trading bot (`/dashboard`) plus bundled API payload (`/operator/live-dashboard`), and captured a screenshot for mk over Telegram.
- mk updated Upbit API keys, but private Upbit calls still failed after app restart with `401 Unauthorized`; likely key/permission/IP-side issue rather than stale process env.
- mk decided to pause Upbit key debugging for now and will re-enter/check the keys from home later; keep this issue open but on hold.
- mk decided to keep the current `investment-bot` version stable and create a separate repo for future multi-symbol / simple rebalancing development, to be merged back later if validated.
- Installed the `agent-browser` skill into `~/.openclaw/skills/agent-browser/` with SKILL.md, reference docs, and templates from the vercel-labs/agent-browser repo; `openclaw skills check` shows it as eligible.
- New browsing preference from mk: whenever web browsing or website interaction is needed, Jarvis should use the `agent-browser` skill/workflow by default.
- Resolved Upbit private API integration for the trading bot by (1) updating `.env` with the actual Notion-stored keys, (2) correcting JWT signing to HS512, and (3) later adding the missing live order path pieces (`create_limit_order`, decimal formatting, and Upbit side mapping `buy/sell -> bid/ask`).
- Upbit read-only/shadow is now working against the real account, and a real live BTC/KRW order was successfully submitted during debugging. Order UUID: `72d2dd77-b7aa-4371-a1d1-1fb34b5f651d`.
- Main trading-bot repo remains the stable operational line; a separate experimental repo was created at `projects/trading-bot-portfolio-lab` for future multi-symbol / simple rebalancing work. Repo intent was documented clearly and committed (`fad1bd5`).
- Added an adjustable auto-trading profile/loop to `investment-bot`, with config under `config/app.yml -> auto_trade` and APIs for status/start/stop/run-once. Initial profile: BTC/KRW, trend_following, 5-minute interval, 15k KRW minimum balance, 20% target allocation, 10k meaningful-order floor, 10-minute pending TTL, 1-cycle cooldown. Commit: `f7f8e12`.
- mk set the proposal-skill development rule: always compare the original/reference skill and the custom Jarvis skill side by side on the same input, and design proposal outputs as revision-capable living documents rather than one-shot final files.
